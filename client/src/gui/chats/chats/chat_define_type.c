#include "home.h"

G_DEFINE_TYPE(Chat, chat, G_TYPE_OBJECT)

static void chat_class_init(ChatClass* klass) {
    if (klass != NULL) {}
}

static void chat_init(Chat *self) {
    if (self == NULL) {}
}
